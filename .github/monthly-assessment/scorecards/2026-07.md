# Docs Monthly Assessment — 2026-07

Top 10 most-visited `docs.spectrocloud.com` pages, assessed by an auto-selected Impersonaid persona (provider `anthropic`, model `claude-sonnet-4-6`). **0 pass · 7 partial · 3 fail.**

> Advisory AI feedback, not a substitute for human review. Assign owners and curate quick wins manually.

| # | Page | Views | Persona | Verdict | Score | Top gaps |
| - | ---- | ----: | ------- | ------- | :---: | -------- |
| 1 | [/deployment-modes/agent-mode/install-agent-host](https://docs.spectrocloud.com/deployment-modes/agent-mode/install-agent-host) | 336 | Platform Engineer | ❌ FAIL | 1 | The page is missing the actual install sequence for the Palette Agent — there is no beginning, no prerequisite checklist, no binary/package download step, and no configuration inputs (credentials, cluster registration token, API endpoints, etc.)<br>The firewall rules block is cut off mid-command, and there is no context explaining the full network topology, required CIDRs, or which rules apply to control-plane vs. worker nodes |
| 2 | [/clusters/public-cloud/aws/required-iam-policies](https://docs.spectrocloud.com/clusters/public-cloud/aws/required-iam-policies) | 320 | Platform Engineer | 🟡 PARTIAL | 3 | The actual policy content (JSON or ARNs) for both Core IAM Policies and Minimum Permissions Policies is absent — the page references them as separate linked pages ("Next: Core IAM Policies") but doesn't include them, so I cannot create the Palette IAM User/Role without navigating elsewhere.<br>There is no prerequisite checklist or required-permissions summary for the IAM entity used to *register* the AWS account with Palette (the page only covers what Palette creates *after* deployment, not what the connecting identity needs beforehand). |
| 3 | [/vm-management](https://docs.spectrocloud.com/vm-management) | 317 | Product Evaluator | 🟡 PARTIAL | 3 | No GA vs. Tech Preview status, support tier, or known limitations are stated anywhere on the page, making it impossible to assess production readiness or adoption risk without following multiple linked sub-pages.<br>There is no mention of licensing model, cost structure, or infrastructure prerequisites (e.g., minimum hardware specs for bare metal), leaving TCO and time-to-value estimates completely unaddressed. |
| 4 | [/vm-management/launchpad-for-vms/install-vmla-iso](https://docs.spectrocloud.com/vm-management/launchpad-for-vms/install-vmla-iso) | 301 | Platform Engineer | 🟡 PARTIAL | 3 | The step-by-step ISO installation sequence (boot process, partitioning, node joining procedure) and the specific fields/inputs for the "Configure Network Settings," "OS and Metrics," "Container and Registry," "OIDC Settings," and "Storage" wizard sections are cut off mid-content, so I cannot determine required inputs (CIDRs, registry endpoints, credentials) or their defaults.<br>The VIP reservation prerequisite references no CIDR constraints, no IPAM requirements, and no guidance on how MetalLB address ranges must be scoped relative to the management network — critical for least-privilege network planning before a single node is booted. |
| 5 | [/clusters](https://docs.spectrocloud.com/clusters) | 269 | Platform Engineer | ❌ FAIL | 2 | There is no step-by-step cluster creation procedure on this page — no required inputs (cloud credentials, network CIDRs, IAM permissions, versions), no prerequisite checklist, and no sequence of actions to follow before clicking anything.<br>The page references cluster profiles as a prerequisite for cluster instantiation but provides no guidance on what a cluster profile must contain or how to create/reference one before attempting cluster creation. |
| 6 | [/tutorials/getting-started](https://docs.spectrocloud.com/tutorials/getting-started) | 264 | Platform Engineer | ❌ FAIL | 1 | No prerequisites checklist, required permissions, supported versions, or network requirements are listed anywhere on the page — a platform engineer cannot begin Day-0 work from this content.<br>The only concrete "first step" offered is booking a sales demo, which is not a technical onboarding path and leaves the setup workflow entirely undefined. |
| 7 | [/vm-management/launchpad-for-vms](https://docs.spectrocloud.com/vm-management/launchpad-for-vms) | 254 | Platform Engineer | 🟡 PARTIAL | 3 | No hardware requirements, network prerequisites (ports, CIDRs), or supported OS/version matrix are listed on this page — I'm told to "review the hardware requirements" but that content lives on a linked page I can't access here.<br>The identity and access management section mentions Keycloak, generic OIDC, and local admin accounts but provides zero configuration inputs (client IDs, redirect URIs, required claims) needed to plan IAM integration before standing up the cluster. |
| 8 | [/clusters/edge](https://docs.spectrocloud.com/clusters/edge) | 232 | Platform Engineer | 🟡 PARTIAL | 2 | No prerequisites checklist or dependency order is provided — I can't determine what must be true (network CIDRs, IAM permissions, supported OS/Kubernetes versions, hardware minimums) before beginning any Edge deployment step.<br>The page links to critical resources (EdgeForge Workflow, Architecture, Deployment Lifecycle, Compatibility Matrix) but none of the actual content from those pages is present here, leaving me unable to proceed without navigating away to find the real setup sequence. |
| 9 | [/vertex](https://docs.spectrocloud.com/vertex) | 227 | Platform Engineer | 🟡 PARTIAL | 2 | No prerequisites, required permissions, network requirements, supported versions, or infrastructure specifications are listed — the page defers everything to sub-pages without summarizing even the minimum inputs needed before contacting support.<br>The access process requires emailing a support team and waiting for credentials/installer image, with no SLA, no self-service path, and no indication of what the installer image format is (OVA, ISO, Helm chart, etc.) or what target platform differences (VMware vs. Kubernetes) actually mean for setup. |
| 10 | [/enterprise-version/install-palette/palette-management-appliance](https://docs.spectrocloud.com/enterprise-version/install-palette/palette-management-appliance) | 196 | Platform Engineer | 🟡 PARTIAL | 3 | The "Install Palette" and "Upload Packs" step-by-step sections are cut off in the provided content (the page jumps from prerequisites mid-sentence into a validation snippet), so I cannot follow the actual installation sequence or TUI walkthrough without navigating elsewhere.<br>The required network ports list is truncated after TCP/443 and TCP/6443, so I cannot confirm all firewall rules that must be open before deployment begins — a hard blocker for my pre-flight checklist. |

<details><summary>Full persona responses</summary>

### /deployment-modes/agent-mode/install-agent-host

VERDICT: FAIL
SCORE: 1
SUMMARY: The page content provided is severely truncated, showing only disconnected fragments of SELinux policy snippets, cgroup v2 checks, and partial firewall rules — with no complete installation sequence, prerequisites list, required inputs, or validation steps visible.

TOP GAPS:
- The page is missing the actual install sequence for the Palette Agent — there is no beginning, no prerequisite checklist, no binary/package download step, and no configuration inputs (credentials, cluster registration token, API endpoints, etc.)
- The firewall rules block is cut off mid-command, and there is no context explaining the full network topology, required CIDRs, or which rules apply to control-plane vs. worker nodes

### /clusters/public-cloud/aws/required-iam-policies

VERDICT: PARTIAL
SCORE: 3
SUMMARY: The page explains what IAM roles Palette creates automatically and the policy-attachment limits, but it doesn't contain the actual policy documents or JSON needed to create the required IAM User/Role before connecting an AWS account to Palette, leaving me unable to complete the least-privilege setup from this page alone.

TOP GAPS:
- The actual policy content (JSON or ARNs) for both Core IAM Policies and Minimum Permissions Policies is absent — the page references them as separate linked pages ("Next: Core IAM Policies") but doesn't include them, so I cannot create the Palette IAM User/Role without navigating elsewhere.
- There is no prerequisite checklist or required-permissions summary for the IAM entity used to *register* the AWS account with Palette (the page only covers what Palette creates *after* deployment, not what the connecting identity needs beforehand).

### /vm-management

VERDICT: PARTIAL
SCORE: 3
SUMMARY: The page gives a clear high-level picture of what VMO does and which deployment path to take, but stops short of the production-readiness signals, licensing/cost context, and support tier details a decision-maker needs to move from awareness to a go/no-go recommendation.
TOP GAPS:
- No GA vs. Tech Preview status, support tier, or known limitations are stated anywhere on the page, making it impossible to assess production readiness or adoption risk without following multiple linked sub-pages.
- There is no mention of licensing model, cost structure, or infrastructure prerequisites (e.g., minimum hardware specs for bare metal), leaving TCO and time-to-value estimates completely unaddressed.

### /vm-management/launchpad-for-vms/install-vmla-iso

VERDICT: PARTIAL
SCORE: 3
SUMMARY: The page covers hardware requirements, Secure Boot prerequisites, and high-level cluster creation flow well, but critical installation steps — specifically the network configuration details, storage configuration, OS/metrics settings, and the actual ISO boot/install sequence — are truncated or missing from the content provided, leaving me unable to complete a full reproducible install.

TOP GAPS:
- The step-by-step ISO installation sequence (boot process, partitioning, node joining procedure) and the specific fields/inputs for the "Configure Network Settings," "OS and Metrics," "Container and Registry," "OIDC Settings," and "Storage" wizard sections are cut off mid-content, so I cannot determine required inputs (CIDRs, registry endpoints, credentials) or their defaults.
- The VIP reservation prerequisite references no CIDR constraints, no IPAM requirements, and no guidance on how MetalLB address ranges must be scoped relative to the management network — critical for least-privilege network planning before a single node is booted.

### /clusters

VERDICT: FAIL
SCORE: 2
SUMMARY: This page describes what Palette does at a high level but provides zero actionable step-by-step instructions for actually creating a cluster, leaving a platform engineer with no executable path forward.
TOP GAPS:
- There is no step-by-step cluster creation procedure on this page — no required inputs (cloud credentials, network CIDRs, IAM permissions, versions), no prerequisite checklist, and no sequence of actions to follow before clicking anything.
- The page references cluster profiles as a prerequisite for cluster instantiation but provides no guidance on what a cluster profile must contain or how to create/reference one before attempting cluster creation.

### /tutorials/getting-started

VERDICT: FAIL
SCORE: 1
SUMMARY: This landing page is purely a navigation hub with no actionable technical content — it provides zero prerequisites, required inputs, setup steps, or IaC hooks needed to actually stand up a cluster.
TOP GAPS:
- No prerequisites checklist, required permissions, supported versions, or network requirements are listed anywhere on the page — a platform engineer cannot begin Day-0 work from this content.
- The only concrete "first step" offered is booking a sales demo, which is not a technical onboarding path and leaves the setup workflow entirely undefined.

### /vm-management/launchpad-for-vms

VERDICT: PARTIAL
SCORE: 3
SUMMARY: This page gives a solid high-level feature overview of the Launchpad for VMs Appliance, but it's purely introductory — it doesn't surface any of the prerequisites, required inputs, or setup sequence I need to actually begin an installation.
TOP GAPS:
- No hardware requirements, network prerequisites (ports, CIDRs), or supported OS/version matrix are listed on this page — I'm told to "review the hardware requirements" but that content lives on a linked page I can't access here.
- The identity and access management section mentions Keycloak, generic OIDC, and local admin accounts but provides zero configuration inputs (client IDs, redirect URIs, required claims) needed to plan IAM integration before standing up the cluster.

### /clusters/edge

VERDICT: PARTIAL
SCORE: 2
SUMMARY: This page is a high-level landing/overview that orients me toward the right resources but provides zero actionable prerequisites, required inputs, or setup steps I can execute right now.
TOP GAPS:
- No prerequisites checklist or dependency order is provided — I can't determine what must be true (network CIDRs, IAM permissions, supported OS/Kubernetes versions, hardware minimums) before beginning any Edge deployment step.
- The page links to critical resources (EdgeForge Workflow, Architecture, Deployment Lifecycle, Compatibility Matrix) but none of the actual content from those pages is present here, leaving me unable to proceed without navigating away to find the real setup sequence.

### /vertex

VERDICT: PARTIAL
SCORE: 2
SUMMARY: This page confirms VerteX exists and is FIPS 140-3 compliant, but provides almost no actionable technical content for a platform engineer trying to begin an installation — it's essentially a marketing landing page with a contact form.
TOP GAPS:
- No prerequisites, required permissions, network requirements, supported versions, or infrastructure specifications are listed — the page defers everything to sub-pages without summarizing even the minimum inputs needed before contacting support.
- The access process requires emailing a support team and waiting for credentials/installer image, with no SLA, no self-service path, and no indication of what the installer image format is (OVA, ISO, Helm chart, etc.) or what target platform differences (VMware vs. Kubernetes) actually mean for setup.

### /enterprise-version/install-palette/palette-management-appliance

VERDICT: PARTIAL
SCORE: 3
SUMMARY: The page gives me enough to understand prerequisites, node sizing, disk layout, and the post-install checklist, but the actual step-by-step "Install Palette" and "Upload Packs" sequences referenced in the TOC are either truncated or not rendered in the provided content, leaving the core installation procedure incomplete.

TOP GAPS:
- The "Install Palette" and "Upload Packs" step-by-step sections are cut off in the provided content (the page jumps from prerequisites mid-sentence into a validation snippet), so I cannot follow the actual installation sequence or TUI walkthrough without navigating elsewhere.
- The required network ports list is truncated after TCP/443 and TCP/6443, so I cannot confirm all firewall rules that must be open before deployment begins — a hard blocker for my pre-flight checklist.

</details>

<sub>Generated 2026-07-02T17:06:38.323Z · Powered by [Impersonaid](https://github.com/spectrocloud/impersonaid).</sub>