.docusaurus
build
node_modules
Dockerfile
src/deprecated
vale/
**/*.yaml
**/*.yml
**/*.mdx
.dockerignore
static/scripts/kapa-widget.bundle.js

docs/api-content/**/*.json
static/apis/**/*.json

# Troublesome files
tsconfig.json
src/components/IconMapper/dynamicFontAwesomeImports.*
static/generated/security-bulletins/cve-reports.md
static/generated/security-bulletins/reports/*.html
.eslintignore
.prettierrc

# Ignore partials
_partials/
scripts/release/

# Frozen copies of a product's docs, created by `docs:version:<id>`. The prettier
# job in pull_request.yaml auto-commits whatever `make format` rewrites, so
# without this it would churn every already-released snapshot on every PR.
# Deliberately correcting a released version is still allowed, and must be
# formatted by hand; docs/contributing/checks-and-ci.md owns that caveat.
# Wildcarded so future products are covered without editing this file.
*_versioned_docs/
*_versioned_sidebars/
*_versions.json

# Generated monthly-assessment scorecards. Prettier's mdx parser reflows the
# AI-written Markdown, and its width-based array collapsing reformats the per-page
# "gaps" lists in a data-dependent way the generator can't reliably match. Both are
# generated artifacts, so ignore them. README.md is hand-authored — keep it checked.
# (pages.json lives one level up and has no collapsible arrays, so the generator's
# trailing newline keeps it Prettier-clean; it is intentionally not ignored.)
.github/monthly-assessment/scorecards/*.md
.github/monthly-assessment/scorecards/*.json
!.github/monthly-assessment/scorecards/README.md

# Claude
.claude/
