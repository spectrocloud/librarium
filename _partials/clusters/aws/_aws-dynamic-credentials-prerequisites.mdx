---
partial_category: clusters-aws-account-setup
partial_name: aws-dynamic-credentials-prerequisites
---

- A {props.edition} account with [tenant admin](/tenant-settings/) access.

- An AWS account with the [required IAM policies](/clusters/public-cloud/aws/required-iam-policies/).

- The ability to create an [IAM role](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_create_for-user.html)
  that {props.edition} can assume using STS. You will create the role while adding your AWS account to {props.edition}.

- (Self-hosted {props.edition} only) By default, adding AWS accounts using STS is disabled. To allow tenants to add AWS accounts using STS, refer to {props.stsGuides === "both" && <a href="/enterprise-version/system-management/configure-aws-sts-account/">Enable Adding AWS Accounts Using STS - Palette</a>}{props.stsGuides === "both" && " or "}<a href="/vertex/system-management/configure-aws-sts-account/">Enable Adding AWS Accounts Using STS - VerteX</a>.