---
partial_category: self-hosted
partial_name: helm-ref-observability
---

{props.version} can export platform metrics to an external observability stack, such as Splunk, using an OpenTelemetry
collector. The observability stack is disabled by default. Set `observability.enabled` to `true` at install or upgrade
time to enable it, and then configure the export from the **Metrics** tab in the system console. For the configuration
procedure, refer to the Export Platform Metrics guide.

| **Parameters**          | **Description**                                                                                                               | **Type** | **Default value** |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------- | -------- | ----------------- |
| `observability.enabled` | Specifies whether to enable the OpenTelemetry observability stack used to export platform metrics to an external destination. | Boolean  | `false`           |

```yaml
observability:
  enabled: true # false by default
```
