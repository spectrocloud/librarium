---
partial_category: self-hosted
partial_name: vmware-environment
---
You can install {props.version} using the Palette Command Line Interface (CLI) on VMware vSphere in either an internet-connected or air-gapped environment. The installation process creates a three-node cluster. Refer to
<PaletteVertexUrlMapper
  edition={props.edition}
  text={`Access ${props.version}`}
  palettePath="/self-hosted-setup/palette#access-palette"
  vertexPath="/self-hosted-setup/vertex#access-palette-vertex"
/> for instructions on requesting repository access.