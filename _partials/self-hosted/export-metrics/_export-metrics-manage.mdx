---
partial_category: self-hosted
partial_name: export-metrics-manage
---

## Pause Export

To pause export, disable the **Export** toggle on the **Metrics** tab. Your Splunk settings are retained, so when you
enable the toggle again, export resumes immediately without re-entering the connection details.

## Exported Metrics

The export includes {props.version} management-plane and Kubernetes metrics, such as CPU, memory, and disk usage.
MongoDB health and replication metrics are also included.

{/* TODO(PEM-11992): This ticket calls out that the curated metric list must be exact, not illustrative. Replace this paragraph with the confirmed metric set from Engineering (Varun / Ravikumar). MongoDB metrics are a "nice-to-have" per PEM-8670, so confirm whether they ship in this release. */}

## Uninstall the Observability Stack

To remove the observability stack, set `observability.enabled` to `false` and upgrade the environment.

```yaml
observability:
  enabled: false
```

When the observability stack is not installed, the backend APIs that support export return an error, and the **Metrics**
tab is read-only.
