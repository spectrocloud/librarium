---
partial_category: self-hosted
partial_name: export-metrics-validate
---

After you save the configuration, export starts automatically within a few seconds. No restart is required.

To confirm that the export is working, search the metrics index you configured in Splunk and verify that metric data
points are arriving. On the **Metrics** tab, the **Export** section also shows a **Connected** status when the connection
is healthy.
