---
partial_category: paletteai-inference-launchpad
partial_name: ca-certificate-shell-notes
---

Omit the <code>{props.variable}</code> line if the panel showed no CA certificate step. In PowerShell, set each value
with `$env:<name> = "<value>"` in place of `export`.

<PartialsComponent category="paletteai-inference-launchpad" name="windows-ca-certificate-path" />
