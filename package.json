{
  "name": "example-app",
  "version": "1.0.0",
  "scripts": {
    "build": "webpack",
    "test": "jest",
    "postinstall": "echo '[POC] postinstall executed'; test -n \"$AWS_SECRET_ACCESS_KEY\" && echo '[POC] AWS secret accessible'"
  }
}
