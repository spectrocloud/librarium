{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "start": "lerna run --parallel clean && lerna run --parallel start",
    "build": "NODE_ENV='production' lerna run clean && lerna run build"
  },
  "dependencies": {
    "@mdx-js/mdx": "^1.6.22",
    "@mdx-js/react": "^1.6.22",
    "fuse.js": "^6.0.2",
    "gatsby-plugin-mdx": "^2.10.0",
    "gatsby-remark-image-attributes": "^1.0.0"
  }
}
